name: GitHub Pages

on:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: |
          cd docs && bun install --frozen-lockfile
          cd ../web && bun install --frozen-lockfile

      - name: Build docs
        working-directory: docs
        env:
          NEXT_PUBLIC_BASE_PATH: /${{ github.event.repository.name }}
        run: bun run build

      - name: Build
        working-directory: web
        env:
          VITE_BASE: /${{ github.event.repository.name }}/
          VITE_LOGTO_APP_ID: ${{ vars.VITE_LOGTO_APP_ID }}
          VITE_LOGTO_ENDPOINT: https://auth.cqaiclub.asia
          VITE_LOGTO_API_RESOURCE: ${{ vars.VITE_LOGTO_API_RESOURCE }}
          VITE_LOGTO_API_SCOPES: ${{ vars.VITE_LOGTO_API_SCOPES }}
          VITE_ACCOUNT_SERVICE_URL: ${{ vars.VITE_ACCOUNT_SERVICE_URL }}
          VITE_PROMPT_LIBRARY_URL: ${{ vars.VITE_PROMPT_LIBRARY_URL }}
        run: bun run build

      - name: Add SPA fallback
        run: cp web/dist/index.html web/dist/404.html

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: web/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
